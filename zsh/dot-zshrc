# 1. Source the Shared Core -------------
f="$HOME/.config/sh-common/common.sh"
[[ -f "$f" ]] && . "$f"
# ---------------------------------------

# 2. Zsh-specific behavior (Completions)
autoload -U compinit && compinit

# 3. Prompt
eval "$(starship init zsh)"

# FZF
eval "$(fzf --zsh)"

# Zoxide
eval "$(zoxide init --cmd cd zsh)"

# ----------------------------------------------------------------
# Must still be move to shell common of project specific files
# export PATH="$HOME/.jenv/bin:$PATH"
# eval "$(jenv init -)"

export ANDROID_HOME=$HOME/Library/Android/sdk
export PATH=$PATH:$ANDROID_HOME/tools:$ANDROID_HOME/platform-tools

# EAS Autocomplete
EAS_AC_ZSH_SETUP_PATH=/Users/anton/Library/Caches/eas-cli/autocomplete/zsh_setup && test -f $EAS_AC_ZSH_SETUP_PATH && source $EAS_AC_ZSH_SETUP_PATH; # eas autocomplete setup
